% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene-reference.R
\name{spaMTPdbGeneReference}
\alias{spaMTPdbGeneReference}
\title{Retrieve a versioned human gene identifier reference}
\usage{
spaMTPdbGeneReference(
  version = "latest",
  local_dir = NULL,
  metadata = FALSE,
  offline = FALSE,
  cache_dir = NULL,
  timeout = 1800,
  retries = 3L
)
}
\arguments{
\item{version}{HGNC archive version or \code{"latest"}, resolved only against the
gene reference registry, independently of \code{\link[=spaMTPdbVersion]{spaMTPdbVersion()}}.}

\item{local_dir}{Directory containing the archived TSV, optionally under a
version subdirectory. Defaults to \code{SpaMTPdb.gene_reference_dir} or the
\code{SPAMTPDB_GENE_REFERENCE_DIR} environment variable.}

\item{metadata}{Return the version registry row without reading/downloading.}

\item{offline}{Use local files or a verified cache only.}

\item{cache_dir}{Download cache root; see \code{\link[=spaMTPdbResource]{spaMTPdbResource()}}.}

\item{timeout}{Download timeout in seconds.}

\item{retries}{Number of verified download attempts.}
}
\value{
A data frame with an attribute \code{spamtp_gene_reference} recording the
HGNC version, organism, source URL and checksum; or a metadata row.
}
\description{
Retrieves a fixed HGNC quarterly archive, independently of the RaMP resource
version. The archive contains approved symbols, previous and alias symbols,
HGNC, Entrez, Ensembl and UniProt identifiers. It is an annotation resource;
experiment data continue to be supplied by SpaMTPData.
}
\details{
Files are resolved from a local directory, verified cache, then the official
HGNC archive URL. Size and MD5 are checked before parsing. No live symbol
queries or changing HGNC "current" downloads are used. The large reference
file is not included in the installed package. This reference covers human
genes and does not implement orthology mapping.
}
\examples{
spaMTPdbGeneReference(metadata = TRUE)
}
