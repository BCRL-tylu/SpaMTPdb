% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resources.R
\name{spaMTPdbResource}
\alias{spaMTPdbResource}
\title{Retrieve one SpaMTP annotation resource}
\usage{
spaMTPdbResource(
  resource,
  version = "latest",
  local_dir = NULL,
  hub = NULL,
  metadata = FALSE,
  offline = FALSE,
  fallback_url = TRUE,
  cache_dir = NULL,
  timeout = 1800,
  retries = 3L,
  verify = TRUE
)
}
\arguments{
\item{resource}{Resource name; see \code{\link[=spaMTPdbResources]{spaMTPdbResources()}}.}

\item{version}{Resource version or \code{"latest"}.}

\item{local_dir}{Optional directory containing staged \code{.rds} resources.}

\item{hub}{Optional pre-created \code{AnnotationHub} object.}

\item{metadata}{Return the registry row without loading the resource.}

\item{offline}{If \code{TRUE}, use local files or a verified cache only; never
query AnnotationHub or Zenodo.}

\item{fallback_url}{If \code{TRUE}, use the immutable Zenodo source URL when the
resource has not yet been ingested into AnnotationHub.}

\item{cache_dir}{Cache directory for source-URL downloads. Defaults to the
platform-specific user cache returned by \code{\link[tools:R_user_dir]{tools::R_user_dir()}}.}

\item{timeout}{Download timeout in seconds for the source-URL fallback.}

\item{retries}{Number of verified download attempts.}

\item{verify}{Verify local files against the published size and checksum.
Set to \code{FALSE} only for intentional development fixtures. Downloaded and
cached files are always verified; loaded object classes are always checked.}
}
\value{
The requested R object, or its registry row when \code{metadata = TRUE}.
}
\description{
Resources are resolved in order: a staged local directory (\code{local_dir}, the
\code{SpaMTPdb.resource_dir} option, or the \code{SPAMTPDB_RESOURCE_DIR} environment
variable), a verified download cache, then the matching AnnotationHub record,
and finally the immutable
Zenodo source URL recorded in the resource manifest. Files retrieved from
Zenodo and local files are verified against the recorded size and MD5
checksum. The verified download cache is usable with \code{offline = TRUE}.
}
\examples{
spaMTPdbResource("chem_props", metadata = TRUE)
}
